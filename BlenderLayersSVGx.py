import bpy
import string

Object = "Curve"
Move = 0.001
Layer = 0
LayNum = 0
def MoveIt():
    if bpy.data.objects.get(Object) is not None:
        bpy.data.objects[Object].location.z += Layer
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

# Copy and Paste it by the number of objects in your SVG file
LayNum = LayNum + 1
Layer = Layer + Move
Object= "Curve." + str(LayNum).zfill(3)
MoveIt()

